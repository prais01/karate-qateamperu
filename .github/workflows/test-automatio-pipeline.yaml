name: Test Automation Pipeline
on:
  push:
    branches:
        - main

jobs:
  practica:
     runs-on: ubuntu-latest

     steps:
      - name: validar examen
        run: echo 'Validando examen...'

